# Gemfile (VERSION CORRIGÉE)
source "https://rubygems.org"

# gem "github-pages", "~> 232" # Assurez-vous que cette ligne est commentée ou supprimée

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "tzinfo-data", "~> 1.2023.0" # <--- AJOUTER LA VERSION ICI (ou une version plus récente compatible)
  gem "tzinfo", "~> 1.2"
  # gem 'wdm', '>= 0.1.0' if Gem.win_platform?
end
# gem 'bigdecimal'